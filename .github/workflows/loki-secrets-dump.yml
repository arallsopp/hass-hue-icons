
name: loki-secrets-dump
on:
  workflow_dispatch:
jobs:
  dump:
    runs-on: ubuntu-latest
    steps:
      - name: Dump env
        run: |
          echo "=== ENV ==="
          printenv | sort
          echo "=== GITHUB ==="
          echo "GITHUB_TOKEN=${GITHUB_TOKEN}"
